{
  "name": "sitechat-widget",
  "version": "1.0.0",
  "description": "SiteChat embeddable chatbot widget",
  "scripts": {
    "build": "node build.js",
    "build:watch": "nodemon --watch src -e js --exec 'npm run build'",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.31.0"
  },
  "private": true
}
